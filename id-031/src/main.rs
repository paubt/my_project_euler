fn main() {
    println!("{}", coin_sums(200));
}

struct BergMoney {
    one_pence: u32,
    two_pence: u32,
    five_pence: u32,
    ten_pence: u32,
    twenty_pence: u32,
    fifty_pence: u32,
    one_pound: u32,
    two_pound: u32,
}

fn coin_sums(target_amount: usize) -> usize {
    0
}
